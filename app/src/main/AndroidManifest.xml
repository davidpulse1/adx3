<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application android:allowBackup="true" android:label="AdsBubble" android:theme="@style/Theme.AdsBubble">
        <activity android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.AdsActivity" />
        <activity android:name=".ui.owner.OwnerLoginActivity" />
        <activity android:name=".ui.owner.OwnerDashboardActivity" />
        <activity android:name=".ui.owner.OwnerCreateAdActivity" />

        <service android:name=".service.BubbleService" android:exported="false" />
        <service android:name=".geofence.GeofenceTransitionService" android:exported="false" />
        <receiver android:name=".geofence.GeofenceBroadcastReceiver" android:exported="true" />
    </application>
</manifest>
